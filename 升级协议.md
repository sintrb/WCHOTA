# WCH BLE OTA 升级协议

本文档根据 `WCHWebOTA.html` 和实际 CH583 设备通信结果整理。

## 1. BLE 接口

| 项目 | UUID |
| --- | --- |
| Primary Service | `0000FEE0-0000-1000-8000-00805F9B34FB` |
| OTA 读写特征 | `0000FEE1-0000-1000-8000-00805F9B34FB` |

所有命令都写入 `FEE1`，所有阶段结果都通过主动读取 `FEE1` 获取。协议不使用 Notify 或 Indicate。

网页端假设 BLE MTU 为 247，因此 ATT 有效载荷为 244 字节。OTA 应用层数据包包含 4 字节包头，固件数据最多 240 字节。

## 2. 固件镜像

升级输入可以是原始 BIN 或 Intel HEX。

- 固件镜像的前 `0x1000` 字节属于引导区，不通过 OTA 写入。
- 实际传输数据为 `image[0x1000:]`。
- 默认起始地址为 `0x1000`，必须是 16 字节对齐地址。
- 升级地址以 16 字节为单位编码：`address_field = address / 16`。
- 地址字段为 16 位小端序。
- HEX 地址空洞转换时填充 `0x00`，与 `hex_to_ota_bin.py` 默认行为一致。

## 3. ImageInfo 查询

### 请求

```text
84 12
```

发送后等待约 500 ms，然后读取 `FEE1`。

### 响应字段

响应通常为 20 字节，关键字段如下：

| 偏移 | 长度 | 含义 | 编码 |
| ---: | ---: | --- | --- |
| 0 | 1 | 当前 Image | `01` = A，`02` = B |
| 1 | 4 | 最大 Image 大小 | 32 位小端整数 |
| 5 | 2 | 擦除 Block 大小 | 16 位小端整数 |
| 7 | 2 | 芯片型号 | 见下表 |

已知芯片型号：

| 芯片 ID | 型号 |
| --- | --- |
| `73 00` | CH573 |
| `79 00` | CH579 |
| `83 00` | CH583 |
| `08 02` | CH32V208 |
| `08 F2` | CH32F208 |

未知芯片 ID 不应直接判定为不可升级，应保留并显示原始 ID。

## 4. 擦除 Flash

### 请求

```text
81 00 addr_lo addr_hi block_count_lo block_count_hi
```

字段说明：

- `addr_lo/addr_hi`：起始地址除以 16 后的 16 位小端值。
- `block_count`：需要擦除的 Block 数量。
- `block_count = ceil(payload_size / block_size)`。

例如，默认从 `0x1000` 开始时，地址字段为 `00 01`。

发送后等待约 4.5 秒，再读取 `FEE1`。响应满足以下条件时表示擦除成功：

```text
响应长度 > 0 且 response[0] == 0
```

## 5. 写入 Flash

### 请求

```text
80 length addr_lo addr_hi data...
```

- `length` 为本包固件数据长度，不包含 4 字节包头。
- `length` 最大为 240。
- `addr` 为本包数据对应的实际 Flash 地址。
- 每个应用层数据包使用一次 BLE 写操作发送，不能任意拆成多个应用层包。
- 推荐使用带响应的 BLE 写入，并等待每包写入完成。

网页协议没有逐包 ACK；BLE 写入完成只表示传输层写操作完成，不代表 Flash 已经完成写入。

## 6. 校验 Flash

### 请求

```text
82 length addr_lo addr_hi data...
```

校验阶段重新发送与写入阶段完全相同的固件数据和地址，只将操作码从 `80` 改为 `82`。设备端将接收数据与 Flash 内容进行比较。

所有校验数据发送完成后等待约 2 秒，再读取 `FEE1`。响应满足以下条件时表示校验成功：

```text
响应长度 > 0 且 response[0] == 0
```

网页协议没有定义主机侧 CRC 或其他校验算法，也没有解释失败响应中的状态码。

## 7. 完成升级

### 请求

固定发送 20 字节：

```text
83 12 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00
```

设备通常在接受该命令后立即切换 Image、复位并断开 BLE 连接。因此发送完成命令后出现断开属于正常现象，不应直接判定为升级失败。

## 8. 完整时序

```text
连接设备
  |
  +-- 写入 84 12
  +-- 等待 500 ms，读取 ImageInfo
  |
  +-- 写入 81 00 ...
  +-- 等待约 4.5 s，读取擦除结果
  |
  +-- 循环写入 80 ... 数据包
  |
  +-- 循环写入 82 ... 数据包
  +-- 等待约 2 s，读取校验结果
  |
  +-- 写入 83 12 + 18 个 00
  +-- 设备复位并断开连接
```

## 9. 注意事项

1. 网页代码曾将擦除块数先截断为低 8 位，导致高字节始终为零；实现时必须正确发送 16 位小端块数。
2. 擦除块数必须使用向上取整，不能在整除时多擦除一个 Block。
3. `ImageInfo` 的最大容量应在传输前检查，镜像超出容量时应终止升级。
4. `FEE1` 必须同时支持写入和读取；如果设备没有该特征，应终止升级。
5. 设备名称可能为空，扫描时可以使用蓝牙地址匹配。
6. `wchota.py` 默认扫描最多 8 秒，但发现匹配设备后会立即进入连接阶段。
